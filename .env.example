# Optional: API base URL when frontend and backend are on different origins (e.g. production).
# VITE_API_BASE_URL=https://your-api.railway.app

# Optional: Amplitude API key for analytics. Get it from amplitude.com → Settings → Projects → Your Project → API Key.
# Without this, all analytics calls are no-ops.
# VITE_AMPLITUDE_API_KEY=your_amplitude_api_key

# --- Backend (server/.env or export before running node server/index.js) ---
# For local magic-link testing: base URL of the app (the link in the email points here).
# Use http://localhost:5173 when running frontend locally so you can copy the link from the terminal.
# APP_ORIGIN=http://localhost:5173

# To actually send magic-link emails (optional for dev): set SMTP env vars.
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-user
# SMTP_PASS=your-pass
# SMTP_FROM="WatermarkFile <noreply@watermarkfile.com>"

# Supabase (user stats + file storage). Get service_role key from Supabase → Settings → API.
# SUPABASE_URL=https://xsqubzhsdxhmstdsxaih.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
